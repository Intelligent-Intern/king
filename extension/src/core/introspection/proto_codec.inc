/*
 * Keeps the protobuf codec runtime grouped in one logical fragment while
 * splitting the former codec monolith into bounded subdomains.
 */

#include "proto_codec/common.inc"
#include "proto_codec/map.inc"
#include "proto_codec/encode.inc"
#include "proto_codec/decode.inc"
