/*
 * Splits the semantic-DNS runtime prelude into lifecycle, export and matching
 * helpers while keeping the shared prelude surface unchanged.
 */

#include "semantic_dns_runtime/lifecycle.inc"
#include "semantic_dns_runtime/export.inc"
#include "semantic_dns_runtime/matching.inc"
