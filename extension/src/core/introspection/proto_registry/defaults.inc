/*
 * Keeps runtime-default helpers grouped while splitting support checks,
 * enum resolution and default persistence into bounded subfragments.
 */

#include "defaults/support.inc"
#include "defaults/enum.inc"
#include "defaults/persist_prepare.inc"
#include "defaults/persist_integer.inc"
#include "defaults/persist.inc"
