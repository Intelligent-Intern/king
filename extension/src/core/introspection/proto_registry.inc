/*
 * Keeps the protobuf registry/runtime helpers grouped in one logical block
 * while splitting the former registry monolith into bounded subdomains.
 */

#include "proto_registry/lookup.inc"
#include "proto_registry/types.inc"
#include "proto_registry/map.inc"
#include "proto_registry/defaults.inc"
