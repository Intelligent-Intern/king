/*
 * Keeps the map codec logic grouped in one logical fragment while splitting
 * storage, encode and decode helpers into bounded subfragments.
 */

#include "map/storage.inc"
#include "map/encode.inc"
#include "map/decode.inc"
