/*
 * Config registration aggregation. The config runtime translation unit includes
 * this module boundary instead of reaching into config-internal registration
 * leaves.
 */

#include "internal/registration.inc"
