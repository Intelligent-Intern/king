/*
 * Config PHP binding aggregation. The config runtime translation unit includes
 * this module boundary instead of reaching into config-internal binding leaves.
 */

#include "internal/php_binding.inc"
