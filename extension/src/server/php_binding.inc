/*
 * Server PHP binding aggregation. The server runtime translation unit includes
 * this module boundary instead of websocket server object-handler leaves.
 */

#include "http1/websocket_server_object_handlers.inc"
