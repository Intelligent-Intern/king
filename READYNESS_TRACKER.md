# King Readiness Log

Purpose:
- This file is a completion log only.
- Open work lives in `BACKLOG.md`.
- `SPRINT.md` is the active sprint extraction from `BACKLOG.md`.
- Do not add open checklists, planning backlogs, or future-work queues here.
- When a batch closes, add a dated log entry with the proof, tests, and release impact.

## 2026-04-22 Backlog Split

- The former mixed readiness/checklist file was converted to a log-only document.
- All open work was moved into `BACKLOG.md` and split into five release batches: video-call demo, E2E/encryption, core runtime/experiment intake, AI/model/fine-tuning, and future MarketView work.
- Old video-call planning sections were removed from this tracker so they no longer conflict with active video-call work.
- `SPRINT.md` is now reserved for whichever backlog slice is actively being executed.
- The split is intentionally parallelizable: video-call demo work owns `demo/video-chat/**`, E2E/encryption owns media security contracts and tests, core runtime owns `extension/**`/`infra/**`/`documentation/**`, AI owns model-inference/fine-tuning surfaces, and MarketView stays future work.

## Completion Log

- 2026-04-23 Q-13 internal proto batch helpers: moved public batch encode/decode dispatch onto `king_iibin_encode_batch()` and `king_iibin_decode_batch()`, with pre-sized output arrays, whole-batch cleanup on first failure, and batch-index exception context that preserves the original lower-level error as `previous`. Added `712-proto-batch-internal-helper-contract.phpt`.

- 2026-04-23 Q-13 public proto batch API: added procedural and `King\IIBIN` batch encode/decode surfaces across stubs, arginfo, externals, function table, public header, and codec dispatch; `711-proto-batch-public-api-surface-contract.phpt` pins the complete surface and whole-batch failure contract.

- 2026-04-23 Q-13 float/double helper consolidation: ported the shared helper intent from `e16af6f` into `extension/include/iibin/iibin_internal.h`, removed duplicate helpers from encode/decode C files, and added `710-proto-float-bit-helper-consolidation-contract.phpt`.

- 2026-04-23 Q-13 ARM64 varint policy: reviewed the experiment ARM64 decode unrolling from `a669b09` and kept it out of the active product path until a guarded architecture profile has benchmark, sanitizer, and parity coverage. Added `709-proto-varint-architecture-policy-contract.phpt` to pin the decision.

- 2026-04-23 Q-13 varint port: manually ported the experiment varint intent from `3267785` and `a669b09` into `extension/include/iibin/iibin_internal.h` with canonical unrolled encode boundaries and uint64 overflow-safe decode. Added `708-proto-varint-boundary-contract.phpt`; local PHPT passed. A full local `make -C extension` is blocked by the existing generated Makefile still requiring `../quiche`, not by the Varint header change.

- 2026-04-23 Q-13 experiment intake provenance: recorded the source commits and author identity for the IIBIN/proto batch and varint work in `documentation/experiment-intake-provenance.md`; subsequent ports must preserve source commit context with `git cherry-pick -x` or explicit source trailers.

- 2026-04-23 HTTP/3 Q-12 migration closure: docs, tests, CI guardrails, release supply-chain checks, and release package manifests now agree on the LSQUIC/BoringSSL stack; Quiche is removed from the active product path and remaining Quiche/Cargo mentions are classified. Proof: `cd extension && php run-tests.php -q tests/698-quiche-main-docs-update-contract.phpt tests/699-quiche-remaining-mentions-contract.phpt tests/700-quiche-cargo-artifact-hygiene-contract.phpt tests/701-ci-builds-http3-stack-contract.phpt tests/702-ci-http3-contract-suite-coverage-contract.phpt tests/703-ci-migration-guardrails-contract.phpt tests/704-release-supply-chain-provenance-pins-contract.phpt tests/705-release-package-manifest-dependency-hashes-contract.phpt tests/706-http3-release-regression-matrix-contract.phpt tests/707-http3-migration-closure-contract.phpt`, plus `./infra/scripts/static-checks.sh`.
- 2026-04-23 HTTP/3 Q-11 regression closure: `extension/tests/http3_release_regression_matrix.inc` and `706-http3-release-regression-matrix-contract.phpt` map the release checklist to 22 pinned PHPT contracts; CI contract-suite coverage now includes one-shot churn and long-duration soak; `documentation/http3-regression-evidence.md` records the LSQUIC/BoringSSL baseline and keeps legacy Quiche artifacts outside the active product path. Proof: `ruby infra/scripts/check-ci-http3-contract-suites.rb`, `cd extension && php run-tests.php -q tests/699-quiche-remaining-mentions-contract.phpt tests/706-http3-release-regression-matrix-contract.phpt`.
- Recent orchestrator closure: worker-loss recovery, deterministic file-worker claim ordering, concurrent claim locking, sustained fairness under contention, real TCP host/port `remote_peer` execution with persisted success/failure snapshots, distributed observability depth, and controller/worker/remote-peer failover harnesses are verified.
- Recent userland orchestrator contract closure: durable tool definitions are separated from executable userland handlers; closures/object graphs/resources/controller memory are non-durable execution state; local, file-worker, and remote-peer execution now use explicit per-process handler registration and fail closed when a peer lacks a required handler.
- Recent handler-registration closure: `king_pipeline_orchestrator_register_handler()` exposes the process-local binding surface over durable tool names and restart-time re-registration is covered by PHPT proof.
- Recent Flow PHP / ETL closure: ETL-on-King is documented as a userland integration layer over object-store, orchestration, telemetry, transport, and runtime-configuration services, with streaming source/sink/checkpoint/execution/failure/partitioning/dataset/serialization/control-plane contracts and end-to-end local/remote proof.
- Recent telemetry closure: metrics, traces, and logs share bounded batch/retry export behavior, request/worker cleanup is hardened, OTLP failure diagnostics are classified, endpoint/credential boundaries are enforced, and exporter queue pressure is observable.
- Recent transport/admin closure: the active tree verifies repo-local HTTP/1, HTTP/2, HTTP/3, QUIC, WebSocket, listener, upgrade, admin-API, CORS/header, fairness, shutdown, and cleanup slices carried by the current runtime.
- Recent coordinated-system closure: aggregate readiness/drain/recovery state gates HTTP/WebSocket/orchestrator/file-worker/remote-peer admission, persists coordinator state under `state_root_path`, and distinguishes clean rolling restart from unclean node-failure takeover.
- Recent QUIC bootstrap closure: pinned LSQUIC, BoringSSL, ls-qpack, and ls-hpack source archives are rehydrated from `infra/scripts/lsquic-bootstrap.lock` without branch-based fallbacks, legacy Quiche bootstrap inputs, or unlocked Cargo retries.
- Recent deterministic-build closure: profile builds snapshot/restore tracked phpize-generated files, sanitizer runtime staging resolves arch-correct Clang runtimes, release packaging records lock-backed provenance hashes, and CI/release package jobs enforce supply-chain checks.
- Recent LSQUIC migration sprint closure: the active HTTP/3 product path targets LSQUIC plus BoringSSL instead of Quiche, with portable `config.m4` detection, deterministic bootstrap/provenance, client/server LSQUIC loaders, one-shot and OO client paths, server listener contracts, QUIC diagnostics, and live `lsquic_conn_get_info()` stats binding.
- Recent Quiche cleanup closure: Quiche loader fallbacks, Quiche build scripts/locks, active product-path Quiche references, unclassified Quiche/Cargo artifacts, and temporary Rust/Cargo HTTP/3 fixtures are removed; remaining Quiche mentions are historical migration notes, release/provenance notes, guard literals, contract-test literals, or migration contracts, and the artifact hygiene gate blocks Quiche/Cargo artifacts.
- Recent CI/release gate closure: `static-checks.sh` chains reproducible package matrix checks, HTTP/3 product-path guards, LSQUIC loader guards, deterministic bootstrap guards, release supply-chain provenance checks, package-manifest dependency hash checks, dependency-provenance docs checks, deterministic HTTP/3 helper-plan checks, and PHP-version matrix alignment.
- Recent experiment-branch intake closure: `origin/experiments/v1.0.6-beta` was audited through `4e58bef`; the current tree already carries the WLVC/WASM/Wavelet/Kalman video-codec surface and a stronger room-bound SFU implementation. Remaining useful work is now tracked in `BACKLOG.md` Batch 3.
- Recent Smart-DNS closure: the DNS-shaped query surface fails closed on undersized response budgets, rehydrates after restart, proves bounded on-wire UDP listener behavior, heals partial durable-state loss without overwriting newer state, and protects mother-node re-election with persisted tombstones.
- Recent autoscaling closure: load-shape explanations, live request-operation signal capture, partial persisted fleet-state recovery against live Hetzner inventory, and fresh-node rollout bootstrap propagation are verified.
- Recent security closure: HTTP body caps, MCP transfer-key truncation fixes, loopback-default peer targeting, object-store metadata bounds, CRLF-safe cloud metadata headers, TOCTOU-safe object-store reads, manifest line caps, snapshot cleanup hardening, remote error metadata bounds, trusted workflow materialization, and Semantic DNS live-probe allowlists are verified.
- Recent video-chat backend dispatch closure: the King PHP demo backend request path is split into focused modules (`runtime`, `auth_session`, `users`, `invites`, `calls`, `realtime`) through `demo/video-chat/backend-king-php/http/router.php`, with router module-order proof.
- Recent video-chat contract-catalog closure: `demo/video-chat/contracts/v1/api-ws-contract.catalog.json` is the canonical versioned REST/WS contract fixture and contract-catalog parity fails fast on drift.
- Recent video-chat auth/session/RBAC closure: login/session/refresh/logout/settings/avatar/admin-user flows have deterministic endpoint contracts, session revocation reaches websocket loops, and route-level RBAC emits stable typed forbidden diagnostics.
- Recent video-chat calls/invites closure: calls list/create/update/cancel, invite create/redeem/copy, schedule metadata, owner/user scoping, participant diff behavior, and protected copy boundaries are proven by backend contracts.
- Recent video-chat realtime closure: websocket gateway validation, room presence snapshots/deltas, chat fanout, reactions, typing, signaling, lobby queue, and room-scoped authorization are covered by realtime contracts.
- Recent video-chat chat/archive/activity closure: inline chat limits, oversized-paste attachment conversion, object-store attachment ACLs, read-only chat archive, speaker/movement awareness, layout modes, and activity strategy state were implemented in the previous video-chat issue batch; remaining release-readiness gaps now live in `BACKLOG.md` Batch 1.
- Recent video-chat SFU/access/Gateway closure: `/sfu` requires explicit room binding, access-link issued sessions persist call/room/user bindings, Gateway join-token and backend signaling mapping contracts exist, and remaining media fanout/security work is now split between `BACKLOG.md` Batch 1 and Batch 2.
- Recent video-chat UI parity closure: the UI parity matrix and protected API semantics matrix are versioned and contract-tested; remaining responsive UI and shared-component cleanup now live in `BACKLOG.md` Batch 1.
- Recent video-chat frontend refactor closure: Q21 split call layout options, shared call-list primitives, call-view state stores, visual-standard guards, shared-surface Playwright smoke coverage, and refactor commit-boundary proof into separate bisectable commits. Proof: `test:contract:call-layout-ui-options`, `test:contract:shared-ui-primitives`, `test:contract:frontend-state-stores`, `test:contract:visual-standards`, `test:contract:refactor-commit-boundaries`, `test:e2e:shared-surfaces`, and `npm run build`.
- Recent AI / SLM closure: real llama.cpp inference, embeddings, RAG, model registry, model-fit selection, vector store, semantic discovery, MCP tool picking, and inference telemetry are proven by contract tests; distributed placement and fine-tuning work now live in `BACKLOG.md` Batch 4.
- Recent model-inference auth closure: optional demo auth for model-inference conversations has bcrypt-backed users, opaque bearer sessions, conversation ownership, and websocket handshake auth while preserving anonymous inference/RAG/discovery access.
