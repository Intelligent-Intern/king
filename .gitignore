libcurl
quiche
llama-fork/
node_modules
.cargo/

# Archived source snapshots
extension/src_bak/

# Local build and cache artifacts
extension/Makefile
extension/Makefile.objects
extension/autom4te.cache/
extension/.libs/
extension/**/.libs/
extension/config.nice
extension/config.log
extension/config.status
extension/configure
extension/configure~
extension/Makefile.fragments
extension/*.la
extension/modules/*.so
extension/modules/*.la
extension/build/profiles/
extension/src/**/*.dep
extension/src/**/*.lo
extension/src/**/*.o
extension/tests/*.diff
extension/tests/*.exp
extension/tests/*.log
extension/tests/*.out
extension/tests/*.php
extension/tests/*.sh
extension/tests/*.mem
extension/tmp-php.ini
extension/run-test-info.php
compat-artifacts/
dist/
!packages/iibin/dist/
!packages/iibin/dist/**
.cache/
.vite/
.codex

# Local machine-specific environment overrides.
.env.local
*.env.local

# Local planning notes that are not part of the public docs set.
docs/future-plan.md

# Model weights MUST NEVER be checked in (they are large and provenance
# belongs on a model registry / object store, not in git). Catch-all
# patterns repo-wide — if you actually need a tiny test fixture that
# overlaps, negate it explicitly under the owning source tree.
*.gguf
*.safetensors
*.pt
*.pth
*.ckpt
*.onnx
*.tflite
*.bin
# Exception: cargo vendor + ring/flate2 test fixtures + quiche cmake
# artifacts need to keep their `.bin` names. These trees are already
# scoped by earlier ignore rules (target/, .cargo/) and are not
# shipped as part of any commit anyway.

**/CMakeFiles/
**/CMakeCache.txt
**/cmake-build-*/
**/cmake_install.cmake
**/compile_commands.json
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / IDE personal state
.idea/
*.code-workspace

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Playwright MCP session artifacts (ephemeral browser logs / page dumps)
.playwright-mcp/

# clangd / IntelliSense (regenerated per build)
compile_commands.json

# Autotools and merge-conflict backup files
*~
*.orig

# Local image artifacts
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
*.tiff
*.svg
!mock/assets/orgas/intelligent-intern/logo.svg
!mock/assets/orgas/intelligent-intern/icon.svg
!mock/assets/orgas/intelligent-intern/icons/*.png
!mock/assets/orgas/kingrt/logo.svg
!mock/assets/orgas/kingrt/icon.svg
!mock/assets/orgas/kingrt/king_logo-withslogan.svg
!mock/assets/orgas/kingrt/icons/*.png
